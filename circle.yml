dependencies:
  override:
    - npm install
    - ./node_modules/bower/bin/bower install

test:
  override:
    - npm run uat

deployment:
  gh-pages:
    branch: master
    commands:
      - sh scripts/sync-gh-pages.sh
